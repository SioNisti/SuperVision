<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:widget="using:SuperVision.Widgets.SessionLapsReached"
             x:Class="SuperVision.Widgets.SessionLapsReached.SessionLapsReachedView"
             x:DataType="widget:SessionLapsReachedViewModel"
             HorizontalAlignment="Stretch">

    <Border Background="{Binding BgColor}" Padding="10,5">
        <StackPanel>
            <TextBlock FontSize="22" FontFamily="Courier New" HorizontalAlignment="Left">Session:</TextBlock>
            
            <ItemsControl ItemsSource="{Binding LapRows}" Foreground="{Binding FontColor}"> 
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="Auto, *">
                    
                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Label}" 
                                       FontSize="22"
                                       FontFamily="Courier New"
                                       HorizontalAlignment="Left"/>

                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Value}" 
                                       FontSize="22"
                                       FontFamily="Courier New"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>