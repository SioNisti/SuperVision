<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:widget="using:SuperVision.Widgets.SessionAttempts"
             x:Class="SuperVision.Widgets.SessionAttempts.SessionAttemptsView"
             x:DataType="widget:SessionAttemptsViewModel"
             HorizontalAlignment="Stretch">
    
<Border Background="{Binding BgColor}" Padding="10,5">
        <Grid ColumnDefinitions="Auto, *">
            <TextBlock Grid.Column="0" 
                       Text="Session:" 
                       Foreground="{Binding FontColor}" 
                       FontSize="22"
                       FontFamily="Courier New"
                       HorizontalAlignment="Left"/>

            <TextBlock Grid.Column="1" 
                       Text="{Binding AttemptRatio}" 
                       Foreground="{Binding FontColor}" 
                       FontSize="22"
                       FontFamily="Courier New"
                       HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</UserControl>